"""MCP server package. Entry point is `agent_mcp/mcp_server.py` (renamed to avoid
shadowing the installed `mcp.server` package). Run via:

    python -m mcp.mcp_server --transport stdio
    python -m mcp.mcp_server --transport sse --port 8765
"""
